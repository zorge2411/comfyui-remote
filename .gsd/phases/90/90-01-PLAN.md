---
phase: 90
plan: 1
wave: 1
---

# Plan 90.1: Bypass Rule and Bypassed/Muted Subgraph Instances

## Objective

Make bypass follow the frontend's `_getBypassSlotIndex` exactly, including the target type and dropping the link when nothing matches. Stop expanding subgraph instances that are bypassed or muted (D-01, D-02). This fixes the C3 cases and most of the remaining C4 cases (`video_wan2_2_14B_s2v`, `flux1_dev_uso_reference_image_gen`, `image_qwen_image_instantx_inpainting_controlnet`).

## Context

- .gsd/phases/90/90-CONTEXT.md
- app/src/main/java/com/example/comfyui_remote/domain/GraphToApiConverter.kt (`bypassInputLink`, `resolveRealSource`, `expandGraphOnce`, `mapInstanceInputs`, pre-scan)
- app/src/test/java/com/example/comfyui_remote/domain/GraphToApiConverterModeTest.kt (fixture style)

## Tasks

<task type="auto">
  <name>Frontend bypass rule with target type</name>
  <files>app/src/main/java/com/example/comfyui_remote/domain/GraphToApiConverter.kt</files>
  <action>
    Add `typesCompatible(a, b)`: true if either is `*` or empty, or the comma-separated lists overlap.
    Replace `bypassInputLink(node, outputSlot)` with `bypassInputLink(node, outputSlot, targetType)`, applying D-01 steps 1–5 over the node's `inputs` (types from each graph slot's `type`) and returning the chosen input's link, or null.
    Extend `resolveRealSource(linkId, targetType, visited)` and pass `targetType` unchanged through bypass and phantom hops.
    At every call site (linked inputs, autogrow slots, dynamic sub-inputs), pass the consuming graph slot's `type`; where it's unknown, use `*`.
    Avoid changing muted or phantom behaviour in this task.
  </action>
  <verify>./gradlew testDebugUnitTest (Windows: gradlew.bat; cloud: plain-JVM mirror)</verify>
  <done>The GraphToApiConverterModeTest cases, old and new, pass.</done>
</task>

<task type="auto">
  <name>Keep bypassed/muted subgraph instances unexpanded</name>
  <files>
    app/src/main/java/com/example/comfyui_remote/domain/GraphToApiConverter.kt
    app/src/test/java/com/example/comfyui_remote/domain/GraphToApiConverterModeTest.kt
    app/src/test/resources/workflow-corpus/known-failures.json
  </files>
  <action>
    In `expandGraphOnce`, when a wrapper's `mode` is 2 or 4, keep the node instead of expanding it, and rewrite it:
    - `inputs` = `definition.inputs` in order, as `{name, type, link}`, taking links from the instance slots through `mapInstanceInputs`;
    - `outputs` = `definition.outputs` as `{name, type}`.
    Leave the External→Wrapper and Wrapper→External link branches alone for this node: links into and out of it stay ordinary links, so the node must not be registered in `wrapperInputRedirects`/`wrapperOutputRedirects`.
    In `convert()`, don't add unexpanded instances (UUID types present in `definitions`) to `missingNodes`.
    Tests:
    (a) bypass picks the same-slot input when it's compatible with both types;
    (b) exact target-type match beats the first compatible input;
    (c) no compatible input drops the link, the `3d_hunyuan3d` shape;
    (d) a `*` target type uses the same slot;
    (e) a chained bypass keeps the final target type;
    (f) a bypassed subgraph instance passes its IMAGE input through to the consumer, and no interior node is emitted;
    (g) a muted subgraph instance's consumers lose the link, and no interior node is emitted;
    (h) a bypassed instance nested inside an expanded subgraph works the same way.
    Run the corpus: `3d_hunyuan3d_multiview_to_model` clears, so remove its entry.
  </action>
  <verify>./gradlew testDebugUnitTest (Windows: gradlew.bat)</verify>
  <done>The 8 tests pass, the corpus is green with 3 entries left (PrimitiveNode), and the full suite passes.</done>
</task>

## Success Criteria

- [ ] No C3 from bypass across the template set
- [ ] `video_wan2_2_14B_s2v`, `flux1_dev_uso_reference_image_gen` and `image_qwen_image_instantx_inpainting_controlnet` no longer lose inputs to bypassed groups
- [ ] Existing Mode, Subgraph and Phase 88 tests unchanged and green
