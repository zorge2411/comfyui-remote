---
phase: 90
plan: 3
wave: 3
---

# Plan 90.3: All-Template Measurement and Unresolved-Source Investigation

## Objective

Make the all-572-template measurement something anyone can re-run, instead of a scratch script. Use it to record the Phase 90 before/after numbers (D-15) and to find the root cause of the links whose source doesn't resolve, which Phase 95 left open (D-13):
- fix a cause here if it's bypass, mute or virtual-node resolution;
- otherwise file it as a new roadmap phase.

## Context

- .gsd/phases/90/90-CONTEXT.md (D-13–D-15)
- .gsd/phases/95/95-SUMMARY.md ("Results", "What's left")
- app/src/test/java/com/example/comfyui_remote/domain/corpus/WorkflowCorpusTest.kt, ApiPromptValidator.kt
- scripts/corpus/select_fixtures.py (docstring: how to get the templates wheel)
- Templates to investigate: `video_wan2_2_14B_s2v` (23 C4), `flux1_dev_uso_reference_image_gen` (4), `image_ernie_image`, `image_ernie_image_turbo` (`PreviewAny.source`), `image_qwen_image_instantx_inpainting_controlnet`

## Tasks

<task type="auto">
  <name>Opt-in all-template report</name>
  <files>
    app/src/test/java/com/example/comfyui_remote/domain/corpus/AllTemplatesReportTest.kt
    app/src/test/resources/workflow-corpus/MANIFEST.md
  </files>
  <action>
    Add a JUnit test that runs only when env `COMFY_TEMPLATES_DIR` points to an unzipped `comfyui_workflow_templates_json/templates/` directory. Use `Assume.assumeTrue`, so normal builds skip it.
    - It converts every `*.json` with the corpus `object_info.json` and runs `ApiPromptValidator`.
    - It prints the clean-prompt count, per-check totals (C1–C7), and the top 20 templates by violation count, with the first violation messages.
    - It writes the same text to `build/reports/all-templates.txt`.
    - It never fails on violations; it's a report.
    Document it in MANIFEST.md in 3 lines: the pip download command, the unzip step, and the gradle command with the env var.
    Get the templates into the scratchpad, not the repo:
    - `pip download --no-deps comfyui-workflow-templates-json`, then unzip.
    - Record the version used.
    - Take the **before** numbers by running at the commit before 90.1 (`git stash` / worktree, or check out `0e9b7c9` into a temp worktree), then the **after** numbers on HEAD.
  </action>
  <verify>set COMFY_TEMPLATES_DIR=... &amp;&amp; gradlew.bat testDebugUnitTest --tests "*AllTemplatesReportTest*" (prints the report); without the env var the test is skipped</verify>
  <done>The report runs on both commits. Before should match Phase 95: 542 clean, C7 29, C3 4. After should show C7 0 and C3 0.</done>
</task>

<task type="auto">
  <name>Root-cause unresolved sources; fix or file</name>
  <files>
    app/src/main/java/com/example/comfyui_remote/domain/GraphToApiConverter.kt
    app/src/test/java/com/example/comfyui_remote/domain/GraphToApiConverterVirtualNodeTest.kt
    .gsd/ROADMAP.md
  </files>
  <action>
    For each of the 5 templates, using the after-report and `CONVERT_DEBUG` output, find the node and link where resolution goes wrong, and compare with the frontend rules in RESEARCH §1–4.
    - **If the cause is muted, bypassed or virtual resolution:**
      - fix it in the converter;
      - add a minimal synthetic test reproducing the shape;
      - re-run the report.
    - **If it's something else** (e.g. subgraph I/O mapping, or a required input left unconnected in the template itself):
      - don't fix it;
      - add a new phase to ROADMAP.md under Milestone 4 with an objective, the evidence (template, node, link) and the numbers.
      - A template that the real ComfyUI frontend would also reject isn't a bug; note it in the summary instead.
    Stop investigating a template after about 30 minutes without a lead, and file it with what's known.
  </action>
  <verify>gradlew.bat testDebugUnitTest (full suite green); rerun AllTemplatesReportTest</verify>
  <done>Each of the 5 templates is fixed (with a test), filed as a new phase, or explained as a template problem, with the evidence recorded.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Phone check and phase summary</name>
  <files>
    .gsd/phases/90/90-SUMMARY.md
    .gsd/STATE.md
    .gsd/ROADMAP.md
  </files>
  <action>
    1. Run `gradlew.bat assembleDebug installDebug`.
    2. Ask the user to run one workflow on the phone that uses a PrimitiveNode or a bypassed node, e.g. the hidream E1.1 template via the Phase 96 template browser if the server has the models, or one of their own. They confirm it queues without a node error.
    3. Write `90-SUMMARY.md` in the Phase 95 format:
       - Delivered: per plan, with commits;
       - where research refined the context (D-04 same-index passthrough, D-10 array order, the saved-value fallback for unresolved widget links);
       - the before/after table (clean, C3, C4, C5, C7, C1) with the templates version;
       - the investigation outcomes;
       - Verification.
    4. Update STATE.md and ROADMAP.md:
       - set Phase 90 to done;
       - tick the must-haves "Frontend-only / virtual nodes …" and "`known-failures.json` is empty";
       - fix the Phase 91 note: missing-node false positives for virtual nodes are gone on the converter side.
  </action>
  <verify>User confirms the phone run; `known-failures.json` is `{}`</verify>
  <done>The summary is written, STATE and ROADMAP are updated, and the phone run is confirmed or explicitly left pending by the user.</done>
</task>

## Success Criteria

- [ ] All-template report is in the repo, opt-in, and documented.
- [ ] C7 = 0 and C3 = 0 across all templates, with before/after recorded.
- [ ] All 5 unresolved-source templates are accounted for: fixed, filed, or explained.
