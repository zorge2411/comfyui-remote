---
phase: 91
plan: 3
wave: 3
---

# Plan 91.3: Pre-flight UI and Device Check

## Objective

Show pre-flight results in the app:
- a reusable grouped issue list;
- a form-screen card that replaces the stored missing-node card;
- the queue-time dialog with **Queue anyway** and **Cancel**.

Then verify on the phone and write the phase summary.

## Context

- .gsd/phases/91/91-CONTEXT.md (D-08–D-11, D-13, D-16)
- .gsd/phases/91/91-02-PLAN.md (`pendingPreflight`, `confirmPreflight`, `cancelPreflight`, form pre-flight)
- app/src/main/java/com/example/comfyui_remote/ui/DynamicFormScreen.kt:
  - `inputs` state, ~82–86;
  - missing-node card, ~124–145;
  - Generate and Queue buttons, ~470–505.
- app/src/main/java/com/example/comfyui_remote/ui/components/ErrorCard.kt (styling to match)

## Tasks

<task type="auto">
  <name>PreflightIssueList component, form card and dialog</name>
  <files>
    app/src/main/java/com/example/comfyui_remote/ui/components/PreflightIssueList.kt
    app/src/main/java/com/example/comfyui_remote/ui/DynamicFormScreen.kt
  </files>
  <action>
    1. **`PreflightIssueList(result: PreflightResult, modifier)`** (D-13): for each node, `heading` in `titleSmall`, then one `bodySmall` line per issue, prefixed with a bullet. Use `onErrorContainer` colours so it sits inside error cards. No truncation; the caller provides the scroll.
    2. **Form card** (D-11): replace the `workflow.missingNodes` card.
       - Collect the form pre-flight from 91.2, keyed on `workflow`, `inputs` and `nodeMetadata`, with a 300 ms debounce.
       - When the result is non-empty, show an `errorContainer` card titled "Server compatibility" with `result.summary()` and an expand toggle (`ExpandMore`/`ExpandLess`) that reveals `PreflightIssueList`. Collapsed by default.
       - No card when the result is null or empty.
    3. **Dialog** (D-08): when `pendingPreflight` is non-null, show an `AlertDialog`:
       - title "This workflow may fail";
       - text: the summary, then `PreflightIssueList` inside a `Column` with `heightIn(max = 360.dp)` and `verticalScroll`;
       - confirm button "Queue anyway" → `confirmPreflight()`;
       - dismiss button "Cancel" → `cancelPreflight()`, also used for `onDismissRequest`.
    4. Remove the now-unused `workflow.missingNodes` reference from the screen. Leave the entity field.
    Avoid:
    - blocking the buttons because of the card: the card is informational (D-08);
    - running the form check on the main thread.
  </action>
  <verify>gradlew.bat assembleDebug testDebugUnitTest</verify>
  <done>Build green; `DynamicFormScreen.kt` has no reference to `missingNodes`; the card and dialog render from ViewModel state.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Phone check and phase summary</name>
  <files>
    .gsd/phases/91/91-SUMMARY.md
    .gsd/STATE.md
    .gsd/ROADMAP.md
  </files>
  <action>
    1. Run `gradlew.bat assembleDebug installDebug`.
    2. Ask the user to check on the phone (D-16):
       a. Open a workflow whose checkpoint isn't on the server (e.g. a template via the Phase 96 browser, or edit the model dropdown's stored value). The "Server compatibility" card shows the missing model, and expanding it lists it under the loader's title.
       b. Tap Generate. The dialog lists the issue. Cancel → nothing queued. Generate again → Queue anyway → the server's own error appears as before (Phase 92 improves that message).
       c. Pick an installed model: the card disappears, and Generate queues with no dialog.
       d. An image workflow with a freshly picked gallery image: no issue about the image.
       e. Optional: the Queue button with the missing model shows the same dialog.
    3. Write `91-SUMMARY.md` in the Phase 90 format:
       - Delivered, per plan with commits;
       - research refinements: form mode skips upload inputs, the Queue-button gate, the runner left unchanged;
       - tests added;
       - the phone result;
       - what's left: Phase 92 reuses `PreflightIssueList`, Phase 97's ernie templates show a C4 until fixed, and uploads are re-done after "Queue anyway".
    4. Update STATE.md, and mark Phase 91 ✅ in ROADMAP.md with a link to the summary.
  </action>
  <verify>The user confirms a–d on the phone; `gradlew.bat testDebugUnitTest` green</verify>
  <done>Summary written; ROADMAP and STATE updated; the user confirmed the phone check.</done>
</task>

## Success Criteria
- [ ] The stored missing-node card is gone; the live card reflects the current inputs and server.
- [ ] The Generate/Queue dialog lists grouped issues; both actions work.
- [ ] Clean workflows show no card and no dialog.
- [ ] Phone check passed; summary written.
