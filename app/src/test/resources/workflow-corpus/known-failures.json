{
 "3d_hunyuan3d_multiview_to_model.json": {
  "checks": [
   "C3"
  ],
  "reason": "Bypassed CLIPVisionEncode (output CLIP_VISION_OUTPUT) has no input of that type; the same-index fallback wires CLIP_VISION into an optional CLIP_VISION_OUTPUT input. The frontend drops the link instead.",
  "phase": [
   90
  ]
 },
 "api_anthropic_claude_sonnet5.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (ClaudeNode.model).",
  "phase": [
   94
  ]
 },
 "api_google_gemini.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (GeminiNodeV3.model).",
  "phase": [
   94
  ]
 },
 "api_kling_o3_i2v.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "api_luma_ray3_3_i2v.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "api_openai_gpt_image_2_image_edit.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (OpenAIGPTImageNodeV2.model).",
  "phase": [
   94
  ]
 },
 "audio_ace_step_1_5_checkpoint.json": {
  "checks": [
   "C7"
  ],
  "reason": "PrimitiveNode is sent to the server instead of being resolved into its targets' widget values.",
  "phase": [
   90
  ]
 },
 "hidream_e1_1.json": {
  "checks": [
   "C4",
   "C7"
  ],
  "reason": "PrimitiveNode sent (C7). ImageScaleToTotalPixels.resolution_steps is newer than the workflow, so widgets_values has no entry for it; the converter omits it instead of using the object_info default (C4).",
  "phase": [
   90,
   95
  ]
 },
 "hunyuan_video_text_to_video.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "image_mage_flow_t2i_int8.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveImageAdvanced.format).",
  "phase": [
   94
  ]
 },
 "image_qwen_Image_2512_controlnet.json": {
  "checks": [
   "C4",
   "C5"
  ],
  "reason": "ResizeImageMaskNode.resize_type is COMFY_DYNAMICCOMBO_V3; its sub-widget values ('scale total pixels' then 1.6) are not expanded, so scale_method gets 1.6. Dynamic-combo sub-inputs are also not emitted (C4).",
  "phase": [
   94
  ]
 },
 "image_to_video_wan.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "template_image_speech_to_video.json": {
  "checks": [
   "C4",
   "C5"
  ],
  "reason": "The control_after_generate value ('fixed') after a seed is consumed by the next widget when that widget is a V3 COMBO (ElevenLabsTextToSpeech.output_format). ResizeImageMaskNode dynamic combo not expanded. Dynamic-combo sub-inputs are also not emitted (C4).",
  "phase": [
   94,
   95
  ]
 },
 "templates-character_sheet.json": {
  "checks": [
   "C5"
  ],
  "reason": "control_after_generate value after seed is consumed by the next V3 COMBO widget (GeminiImage2Node.aspect_ratio = 'fixed'), shifting every later widget.",
  "phase": [
   95
  ]
 },
 "text_to_video_wan.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "utility-gan_upscaler.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "utility_depth_anything3_image_depth_estimation.json": {
  "checks": [
   "C4"
  ],
  "reason": "ImageCompare.compare_view is newer than the workflow (empty widgets_values); the object_info default is not used.",
  "phase": [
   95
  ]
 },
 "utility_depth_anything3_video_depth_estimation.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (DA3Render.output, SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "utility_image_stitch.json": {
  "checks": [
   "C4",
   "C5"
  ],
  "reason": "ResizeImageMaskNode.resize_type COMFY_DYNAMICCOMBO_V3 sub-widgets not expanded; scale_method gets 2048. Dynamic-combo sub-inputs are also not emitted (C4).",
  "phase": [
   94
  ]
 },
 "utility_topaz_illustration_upscale.json": {
  "checks": [
   "C7"
  ],
  "reason": "PrimitiveNode is sent to the server.",
  "phase": [
   90
  ]
 },
 "video_kandinsky5_t2v.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "video_ltx2_depth_to_video.json": {
  "checks": [
   "C4",
   "C5"
  ],
  "reason": "ResizeImageMaskNode.resize_type COMFY_DYNAMICCOMBO_V3 sub-widgets not expanded; scale_method gets 1280. Dynamic-combo sub-inputs are also not emitted (C4).",
  "phase": [
   94
  ]
 },
 "video_wan2.1_fun_camera_v1.1_1.3B.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 },
 "video_wan2_2_5B_ti2v.json": {
  "checks": [
   "C4"
  ],
  "reason": "COMFY_DYNAMICCOMBO_V3 sub-inputs of the selected option are never emitted (SaveVideo.format).",
  "phase": [
   94
  ]
 }
}
